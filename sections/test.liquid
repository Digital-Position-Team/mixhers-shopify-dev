{% render 'shared-product-details', then_render: 'testing'',  product: product, %}
{{  product.recharge}}
{% schema %}
  {
    "name": "test",
    "settings": [],
    "presets":[
      {
        "name":"testing"
      }
    ]
  }
{% endschema %}