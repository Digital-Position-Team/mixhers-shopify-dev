{% schema %}
{
  "name": "Wholesale Hero",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Wholesale Program"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Mixhers is on a mission..."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Apply Now"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL",
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#f5f5f5"
    }
  ]
}
{% endschema %}
